# OpenCoachAI 에이전트 작업 규칙

이 파일은 저장소에서 작업하는 모든 AI 에이전트가 따라야 하는 기본 규칙이다.
사용자의 최신 명시적 요청이 이 문서보다 우선한다.

## 1. 작업을 설명하는 방법

- 작업을 시작하기 전에 현재 상태, 이번 목표, 변경 예정 범위를 한국어로 짧게 설명한다.
- 어려운 용어를 처음 사용할 때는 쉬운 비유와 정확한 기술 설명을 함께 제공한다.
- 코드에서 직접 확인한 사실과 앞으로 제안하는 설계를 구분해서 말한다.
- 범위를 임의로 넓히지 않는다. 새로운 범위가 필요하면 이유와 영향을 먼저 알린다.
- 작업이 끝나면 변경 파일, 사용자 관점의 변화, 검증 결과, 남은 위험을 보고한다.

## 2. 구현 원칙

- 변경 전에 관련 코드, 테스트, 문서를 먼저 읽는다.
- 한 번에 하나의 최소 기능 또는 하나의 버그 원인만 변경한다.
- 기존 동작을 바꿀 때는 테스트를 함께 추가하거나 수정한다.
- 프론트엔드와 백엔드의 API 계약은 타입, 스키마, 테스트, 문서를 함께 맞춘다.
- 실제로 측정한 값과 추정한 값을 이름과 UI에서 명확히 구분한다.
- 근거 데이터 없이 STT 모델, 프롬프트, 임계값을 더 좋다고 단정하지 않는다.

## 3. 음성·AI 데이터 안전 규칙

- API 키, 원본 음성, 전사문, 사용자 프로필, 프롬프트 전문을 로그에 남기지 않는다.
- 음성 파일을 저장해야 한다면 목적, 위치, 보존 기간, 삭제 방법을 먼저 문서화한다.
- 운영 사용자 음성을 테스트 데이터로 재사용하지 않는다.
- STT가 생성한 원문과 사용자가 수정한 전사문을 서로 덮어쓰지 않는다.
- 전사문만으로 발음, 강세, 음가, 실제 음질을 판단하지 않는다.
- 브라우저 음량 기반 지표는 음성학적 측정값이 아니라 휴리스틱임을 표시한다.

## 4. Git 브랜치 규칙

- `main`: 배포 가능한 안정 버전만 둔다.
- `develop`: 다음 배포를 위한 통합 브랜치로 사용한다.
- 에이전트 작업 브랜치는 `codex/<종류>-<짧은-주제>` 형식을 사용한다.
- 예: `codex/feat-transcription-confidence`, `codex/fix-empty-transcript`.
- 현재 기능 기준선이 `develop`에 아직 합쳐지지 않았다면, 사용자가 지정한 기준 브랜치에서 분기하고 그 사실을 보고한다.
- 공유 브랜치에 강제 push하지 않는다.
- 서로 다른 목적의 변경을 한 브랜치에 계속 쌓지 않는다.

## 5. 커밋 규칙

커밋 제목은 아래 형식을 사용한다.

```text
[종류] 한글로 작성한 구체적인 변경 제목
```

허용하는 기본 종류:

- `[feat]`: 새로운 사용자 기능
- `[fix]`: 잘못된 동작 수정
- `[refactor]`: 외부 동작을 바꾸지 않는 구조 개선
- `[test]`: 테스트 또는 평가 도구 변경
- `[docs]`: 문서 변경
- `[chore]`: 빌드, 의존성, 개발 환경 변경

모든 커밋에는 제목뿐 아니라 다음 내용을 포함한 본문이 있어야 한다.

```text
[feat] 낮은 신뢰 전사 구간 표시

- 전사 응답의 신뢰도 구간을 화면 모델로 변환
- 사용자가 확인해야 할 문장을 시각적으로 표시
- 응답 매핑과 화면 동작 테스트 추가

검증:
- npm test
- npm run build
```

- 제목은 `오류 해결`, `수정`, `작업`처럼 원인을 알 수 없는 표현을 사용하지 않는다.
- 본문에는 실제로 변경한 사항을 항목별로 나열한다.
- 실행한 검증 명령과 결과를 `검증:` 아래에 기록한다.
- 한 커밋은 되돌려도 다른 기능이 깨지지 않는 최소 논리 단위여야 한다.
- 하나의 API 계약을 완성하는 데 프론트엔드, 백엔드, 테스트 변경이 모두 필요하면 함께 커밋할 수 있다.
- 관련 없는 포맷 변경이나 파일 정리를 기능 커밋에 섞지 않는다.

## 6. 검증 규칙

- 프론트엔드 변경은 가능한 범위에서 `npm test`, `npm run lint`, `npm run build`를 실행한다.
- 백엔드 변경은 가능한 범위에서 `python -m pytest`, `python -m ruff check .`, `python -m compileall app`을 실행한다.
- 실행 환경이나 의존성이 없어 검증하지 못했다면 성공으로 표현하지 않고 정확한 원인을 보고한다.
- 외부 AI 호출을 사용하는 테스트는 기본 테스트에서 비용이 발생하지 않도록 가짜 provider를 사용한다.
- 모델 또는 프롬프트 변경은 고정된 STT 평가 자료의 전후 결과를 비교한 뒤 채택한다.

## 7. 문서 동기화 규칙

- 전체 안내는 루트 `README.md`와 `docs/README.md`에서 찾을 수 있어야 한다.
- 프론트엔드 동작은 `docs/frontend/`, 백엔드 동작은 `docs/backend/`에 기록한다.
- 시스템 전체 흐름은 `docs/architecture/`, AI 도메인 지식과 평가법은 `docs/ai/`에 기록한다.
- 중요한 설계 선택과 대안은 `docs/decisions/`의 ADR로 남긴다.
- 코드와 문서가 다르면 코드가 현재 동작의 근거다. 차이를 발견한 작업에서 문서도 함께 고친다.
